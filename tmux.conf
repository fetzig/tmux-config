# to fix nvim theme colors when starting nvim within tmux session
# set-option -sa terminal-overrides ",xterm*,Tc"
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB,xterm-ghostty:RGB"
set -as terminal-features ',xterm-ghostty:extkeys'
set -g extended-keys always
set -g extended-keys-format csi-u
set -g allow-passthrough on

set -g mouse on

# set prefix
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Shift Alt vim keys to switch windows
# bind -n M-H previous-window
# bind -n M-L next-window

# Catppuccin flavor will be set dynamically by tmux-dark-notify
# set -g @catppuccin_flavor 'mocha'


set -g @catppuccin_window_status_style "custom"
set -g @catppuccin_window_right_separator " █"
set -g @catppuccin_window_current_right_separator " █"
set -g @catppuccin_window_number_position "right"
set -g @catppuccin_window_middle_separator " | "

set -g @catppuccin_window_default_fill "none"

set -g @catppuccin_window_current_fill "all"

set -g @catppuccin_window_current_text " #{b:pane_current_path}"
set -g @catppuccin_window_text " #{b:pane_current_path}"

set -g @catppuccin_status_left_separator "█"
set -g @catppuccin_status_right_separator "█"

set -g @catppuccin_date_time_text "%Y-%m-%d %H:%M:%S"


set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'catppuccin/tmux'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'erikw/tmux-dark-notify'
set -g @plugin 'tmux-plugins/tmux-resurrect'

# Ghostty maps Shift+Enter to LF (Ctrl-J) for pi multiline input.
# Avoid capturing Ctrl-J in tmux root table via vim-tmux-navigator.
set -g @vim_navigator_mapping_down "M-j"

# tmux-resurrect configuration
# Only restore pane layouts and working directories, not running programs
set -g @resurrect-processes 'false'
set -g @resurrect-capture-pane-contents 'off'

# Configure tmux-dark-notify to switch between Catppuccin Latte and Mocha
set -g @dark-notify-theme-path-light '$HOME/.config/tmux/themes/catppuccin_latte.conf'
set -g @dark-notify-theme-path-dark '$HOME/.config/tmux/themes/catppuccin_mocha.conf'

set-environment -g TMUX_PLUGIN_MANAGER_PATH "$HOME/.config/tmux/plugins/"

run '~/.config/tmux/plugins/tpm/tpm'

# Ensure Ctrl-J reaches applications (Ghostty Shift+Enter emits LF/Ctrl-J).
unbind -n C-j

# Source theme file managed by tmux-dark-notify (fallback if plugin hasn't run yet)
# Theme file handles: flavor selection, catppuccin loading, status bar config, and battery plugin
if-shell "test -e ~/.local/state/tmux/tmux-dark-notify-theme.conf" \
     "source-file ~/.local/state/tmux/tmux-dark-notify-theme.conf" \
     "source-file ~/.config/tmux/themes/catppuccin_mocha.conf"

# set vi-mode
set-window-option -g mode-keys vi
# keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle

# open panes in current directory
bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

# bind -n M-k clear-history
